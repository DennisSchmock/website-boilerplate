import styled from 'styled-components'

const Background = styled.div`
  width: 100vw;
  height: 100vw;
  display: flex;
  overflow: hidden;
  z-index: 1;
  position: absolute;
`

export default { Background }
