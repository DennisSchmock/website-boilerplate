import styled from 'styled-components'

const Content = styled.div`
  background-color: black;
`

const ContentWrapper = styled.div`
  background-color: black;
`

export default { Content, ContentWrapper }
